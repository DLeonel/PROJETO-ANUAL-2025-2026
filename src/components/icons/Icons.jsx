/*import style from "./icons.module.css";

export const Icon = ( variant, name, id ) => {

    return <i className={`bi  bi-${style.name} ${style.variant} ${style.id}`}></i>

}

export const BtnIcon = ( label, variant, name, id ) => {

    return <i role="button" aria-label={ label } className={`bi  bi-${style.name} ${style.variant} ${style.id}`}></i>

}*/